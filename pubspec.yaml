name: mqtt5_client_manually_ack
description: MQTT5 client with custom manual QoS 1 acknowledgement support.
version: 1.0.0

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  typed_data: "^1.4.0"
  encrypt: ^5.0.3
  event_bus: "^2.0.1"
  meta: "^1.15.0"
  characters: "^1.4.0"
  flutter_secure_storage: "^9.2.0"
  http: ^1.1.0
  flutter_cache_manager: ^3.4.1

dev_dependencies:
  test: "^1.25.8"
